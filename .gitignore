node_modules/
demo-workspace.json
uno.json
weave.json
weaver.json
*.db
*.db-wal
*.db-shm
files/
*.tmp
.DS_Store
.impeccable/
test.json

# Cloudflare deployment (Feature #84): real config and secrets never land in
# the public repo — commit wrangler.toml.example only. Entries exist BEFORE the
# files do (the Issue #10 class, one layer up).
wrangler.toml
wrangler.jsonc
.dev.vars
.wrangler/

# Merge-tool debris and throwaway browser probes: an .orig is a byte copy of
# the file beside it (194KB of duplicated app.js the last one), and the probes
# are one-off Playwright scripts, not tests.
*.orig
.*-probe.mjs
.*-shot.mjs
.wt-slot/
