v0.4.4: seven captured issues fixed, and every build now ships its issue list

Issues #122-#128, captured 2026-08-31 from the work-instance tracker and
fixed in one build:
- #122 workspace delete: DELETE /api/workspaces/:ref + hold-to-confirm on
  the workspace page; the .db moves to <dataDir>/trash/, recoverable; the
  default and weave docs workspaces refuse
- #123 a hub-created workspace opens with a description naming first steps
- #124 the empty-schema banner is gone
- #125 Shift+Enter inside a grid cell commits and creates another record,
  focus landing in the new row's Name cell
- #126 the registry registers itself: the Workspace space and the four
  system tables are rows in Spaces/Tables; deleting or repurposing them is
  refused (guards in interceptCreate; deleteSpace/deleteTable already
  refuse). Registry tests select rows by name, not index.
- #127 percent means percent: stored fraction, displayed x100, input /100,
  with a one-time /100 migration so existing cells read unchanged
- #128 formula chips insert [bracketed] tokens whenever the name is not a
  safe bare identifier (spaces, keywords, function-name collisions)

Development sync (Feature #152): scripts/export-development.mjs exports the
canonical Development space to docs/development.json at release; weave serve
applies it to the local docs workspace on boot (syncDevelopment: upsert by
name, shipped status wins, locally filed rows untouched, one write per
build via a meta stamp). test/development-sync.test.mjs pins the manifest
to the package version, so a release without a fresh export fails the suite.

Merged over 5afc889 (structure trash): registry-row soft delete stays soft;
the system rows refuse hard delete; structure-trash purge test counts only
user rows.

Full suite: 1080 pass, 0 fail.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
